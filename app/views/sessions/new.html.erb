<% provide(:title, 'Login') %>

<%= form_for(:session, url: signin_path) do |f| %>
  <%= f.label :email %>:
  <%= f.email_field :email %>
  <br>
  <%= f.label :password %>:
  <%= f.password_field :password %>

  <%= f.submit 'Login' %>
<% end %>
